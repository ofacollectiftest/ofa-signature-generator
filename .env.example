# --- GitHub upload (public repo recommended) ---
GITHUB_TOKEN=ghp_xxx_or_fine_grained_token
GITHUB_OWNER=ofacollectiftest
GITHUB_REPO=ofa-signatures
GITHUB_BRANCH=main

# Where to store member photos in the repo
GITHUB_PATH_PREFIX=members

# Optional: if you serve assets via GitHub Pages or a CDN, set this.
# Otherwise the app will use raw.githubusercontent.com
ASSET_BASE_URL=
